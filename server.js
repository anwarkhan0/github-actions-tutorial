const app = require("./src/app");
const PORT = process.env.PORT || 3000;

// Start the server.........aaacvcsdfsdfsasfasf
app.listen(PORT, () => {
  console.log(`Server running on port ${PORT}`);
});

